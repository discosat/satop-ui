# Authentication & Authorization Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    NEXT.JS APPLICATION                           │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    middleware.ts                          │  │
│  │  • Intercepts all requests                                │  │
│  │  • Checks Auth0 session                                   │  │
│  │  • Redirects unauthenticated users to /login             │  │
│  │  • Handles Auth0 routes (/auth/*)                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                         │                                         │
│                         ▼                                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  app/layout.tsx                           │  │
│  │  • Calls getSession()                                     │  │
│  │  • Fetches Auth0 session + backend user data             │  │
│  │  • Provides SessionContext to all components              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                         │                                         │
│                         ▼                                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │             Page/Layout Components                        │  │
│  │  • Server: requireAdmin(), requireOperator(), etc.       │  │
│  │  • Client: useIsAdmin(), useIsOperator(), Protected      │  │
│  │  • Check role requirements                                │  │
│  │  • Redirect if insufficient permissions                   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
└────────────┬────────────────────────────┬────────────────────────┘
             │                            │
             ▼                            ▼
┌────────────────────────┐    ┌──────────────────────────┐
│       Auth0            │    │   Your Backend API       │
│  • Authentication      │    │  • User data + roles     │
│  • Token generation    │    │  • /users/me endpoint    │
│  • OAuth flow          │    │  • Token validation      │
└────────────────────────┘    └──────────────────────────┘
```

## Authentication Flow

```
1. User visits /platform
       │
       ▼
2. middleware.ts intercepts
       │
       ├─ No Auth0 session?
       │     │
       │     └──> Redirect to /login
       │
       └─ Has Auth0 session?
             │
             └──> Continue to page
                    │
                    ▼
3. Layout calls getSession()
       │
       ├──> Auth0: Get session + token
       │
       └──> Backend: Call /users/me with token
              │
              └──> Returns user + role
                     │
                     ▼
4. Check role requirements
       │
       ├─ Insufficient role?
       │     │
       │     └──> Redirect to /unauthorized
       │
       └─ Sufficient role?
             │
             └──> Render page ✅
```

## File Structure & Responsibilities

```
discosat/
│
├── middleware.ts                    ← 🔐 Route protection (Auth0)
│
├── lib/
│   ├── auth0.ts                     ← ⚙️  Auth0 client config
│   ├── session.ts                   ← 🔑 Session management (Auth0 + Backend)
│   ├── authorization.ts             ← 🎯 Role checking utilities
│   ├── auth-helpers.ts              ← 🛠️  Server action helpers
│   └── types.ts                     ← 📝 TypeScript types
│
├── app/
│   ├── layout.tsx                   ← 🌐 Root layout with SessionProvider
│   ├── context.tsx                  ← 📦 Session context for client components
│   │
│   ├── login/
│   │   └── login-form.tsx           ← 🔓 Auth0 login button
│   │
│   ├── unauthorized/
│   │   └── page.tsx                 ← ⛔ Access denied page
│   │
│   ├── platform/                    ← 👁️  VIEWER+ only
│   │   └── layout.tsx               ← Add: await requirePlatformAccess()
│   │
│   └── administration/              ← 👑 ADMIN only
│       └── layout.tsx               ← Add: await requireAdmin()
│
├── components/
│   ├── protected.tsx                ← 🛡️  Client-side protection + hooks
│   ├── logout-button.tsx            ← 🚪 Logout component
│   │
│   └── sidebar/
│       ├── app-sidebar.tsx          ← 📱 Sidebar with role requirements
│       ├── nav-main.tsx             ← 🧭 Navigation with role filtering
│       └── nav-user.tsx             ← 👤 User profile with role display
│
└── app/api/
    └── api-client.ts                ← 🌐 API client with auto token injection
```

## Role-Based Access Control

```
┌─────────────────────────────────────────────────────────────┐
│                        ADMIN (Level 3)                       │
│  ✅ Full access to everything                                │
│  ✅ Administration pages                                      │
│  ✅ Platform pages                                            │
│  ✅ All operations                                            │
└─────────────────────────────────────────────────────────────┘
                         inherits ↓
┌─────────────────────────────────────────────────────────────┐
│                      OPERATOR (Level 2)                      │
│  ✅ Platform pages                                            │
│  ✅ Execute operations                                        │
│  ❌ Administration pages                                      │
└─────────────────────────────────────────────────────────────┘
                         inherits ↓
┌─────────────────────────────────────────────────────────────┐
│                       VIEWER (Level 1)                       │
│  ✅ Platform pages (read-only)                               │
│  ❌ Execute operations                                        │
│  ❌ Administration pages                                      │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow

```
┌──────────────┐
│ User clicks  │
│   Login      │
└──────┬───────┘
       │
       ▼
┌──────────────────────┐
│  /auth/login         │
│  (Auth0 SDK)         │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  Auth0 OAuth Flow    │
│  (External)          │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  /auth/callback      │
│  (Auth0 SDK)         │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  Session Created     │
│  with Access Token   │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────────────┐
│  User accesses protected page│
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  getSession() called          │
│  ├─ Get Auth0 session        │
│  └─ Call /users/me           │
│     └─ Returns user + role   │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│  Check role requirements      │
└──────┬───────────────────────┘
       │
       ├─ ✅ Access granted
       │     │
       │     └──> Render page
       │
       └─ ❌ Access denied
             │
             └──> Redirect to /unauthorized
```

## API Request Flow

```
┌────────────────────────┐
│ Component calls        │
│ apiClient.get('/users')│
└──────────┬─────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ apiFetch() in api-client.ts      │
│ ├─ Get Auth0 session             │
│ ├─ Extract access token          │
│ └─ Add Authorization header      │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ fetch() to Backend API           │
│ Headers:                          │
│   Authorization: Bearer <token>  │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ Backend validates token          │
│ ├─ Verify with Auth0             │
│ └─ Process request               │
└──────────┬───────────────────────┘
           │
           ▼
┌──────────────────────────────────┐
│ Response returned to component   │
└──────────────────────────────────┘
```

## Security Layers

```
Layer 1: middleware.ts
         └─> Authentication (Auth0 session)
             ✅ Ensures user is logged in

Layer 2: Server Components (Layouts/Pages)
         └─> Authorization (Role checking)
             ✅ Ensures user has required role
             ✅ Server-side = Secure

Layer 3: Client Components
         └─> UX Enhancement (Show/hide UI)
             ✅ Better user experience
             ⚠️  NOT for security (client can be manipulated)

Layer 4: Backend API
         └─> Final validation
             ✅ Token validation
             ✅ Permission checking
             ✅ Last line of defense
```

## Key Components Interaction

```
┌─────────────────────────────────────────────────────────────┐
│                      Browser Client                          │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ useSession() hook                                      │  │
│  │   ↓ gets data from                                     │  │
│  │ SessionContext (from app/context.tsx)                  │  │
│  │   ↓ provided by                                        │  │
│  │ SessionContextProvider in app/layout.tsx               │  │
│  │   ↓ with data from                                     │  │
│  │ getSession() (lib/session.ts)                          │  │
│  │   ↓ which combines                                     │  │
│  │ Auth0 session + Backend user data                      │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Protected component / hooks                            │  │
│  │   • useIsAdmin()                                       │  │
│  │   • useIsOperator()                                    │  │
│  │   • <Protected requireAdmin>                           │  │
│  │     ↓ uses                                             │  │
│  │   canAccessAdministration(role)                        │  │
│  │     ↓ from                                             │  │
│  │   lib/authorization.ts                                 │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
└───────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      Server Side                             │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Server Component/Page                                  │  │
│  │   await requireAdmin()                                 │  │
│  │     ↓ from lib/auth-helpers.ts                        │  │
│  │     ↓ calls                                            │  │
│  │   getSession()                                         │  │
│  │     ↓ which calls                                      │  │
│  │   auth0.getSession() + GetUserMe()                     │  │
│  │     ↓ checks with                                      │  │
│  │   canAccessAdministration(role)                        │  │
│  │     ↓ if fails                                         │  │
│  │   redirect("/unauthorized")                            │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

## Summary

1. **Authentication** (Who you are): Auth0
2. **Authorization** (What you can do): Your backend + role system
3. **Session**: Combines both (Auth0 token + backend user/role)
4. **Protection**: Multiple layers (middleware → server → client)
5. **API calls**: Automatic token injection via apiClient

This architecture provides secure, role-based access control with a great user experience!
